[project]
name = "windows-internal-recorder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "pyinstaller>=6.17.0",
    "soundcard>=0.4.5",
    "soundfile>=0.13.1",
]
